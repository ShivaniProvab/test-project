type User {
  _id: String!
  username: String!
  email:String!
  password: String!
}

input UserInput {
  username: String!
  email:String!
  password: String!
}

type Query {
  hello: String!
  users: [User!]
  test:String!
}

type Mutation {
  createUser(input: UserInput!): User
}